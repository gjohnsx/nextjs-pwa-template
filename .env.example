# Optional: needed only if you want to generate icons via favicon-generator skill.
OPENAI_API_KEY=

# Optional: needed only if you want web push notifications.
# Generate values with: bun run vapid:generate
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
